import type { Lang, LayoutMode, ThemeMode } from '@/types/setting'

export const DEFAULT_LANG: Lang = 'zh-CN'

export const DEFAULT_THEME: ThemeMode = 'light'

export const DEFAULT_LAYOUT: LayoutMode = '2-column'
